<?php
/**
 * Object cache drop-in — no-op until an object-cache plugin replaces this file.
 * WordPress falls back to the default in-memory cache when nothing else is loaded.
 */
if ( ! defined( 'ABSPATH' ) ) {
	exit;
}
